# CS263-FinalProject

This uses llvm as a submodule. 

## Docker
I included a Dockerfile to build a standardized Ubuntu container.
From the SensiTaint directory, run `make docker-build` to build the container.
Then, run `make docker-run` to run the container.